import {AbstractComposition} from "../Abstract/AbstractComposition";

export class Intro extends AbstractComposition {

    setup(animation, ...payload) {

    }

    stage(scene, camera, objects) {
        console.log('yeah')
    }

    update(scene, camera, objects, time, delta) {

    }

}
